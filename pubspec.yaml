name: helix
description: The DNA engine - resolves DNA packages declared as dev-dependencies, merges their dna folders and instantiates configs, docs, scripts and agent skills into the target repo.
version: 1.3.1
repository: https://github.com/ggdna/helix.git

environment:
  sdk: ">=3.13.0 <4.0.0"

dependencies:
  args: ^2.7.0
  gg_args: ^3.2.0
  gg_console_colors: ^2.5.0
  gg_hash: ^1.3.0
  gg_json: ^4.3.0
  gg_log: ^1.2.0
  gg_process: ^1.3.0
  path: ^1.9.1
  yaml: ^3.1.4

dev_dependencies:
  lints: ^6.1.0
  test: ^1.32.0

executables:
  helix: